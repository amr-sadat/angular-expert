# i18n.md + SKILL.md hookup

Status: ready-for-agent
Type: AFK

## Parent

`.scratch/angular-20-missing-features/PRD.md`

## What to build

Create `references/i18n.md` covering `@angular/localize` and wire it into `SKILL.md`. Match the depth, density, and style precedent set by the animations pilot (issue #01).

Topics for `references/i18n.md`:

- `@angular/localize` setup and `ng add @angular/localize`
- `$localize` tagged template literals for TypeScript-side messages
- `i18n` attribute usage in templates (meaning, description, ID)
- Message extraction via `ng extract-i18n` and the XLIFF/XMB/JSON output formats
- Runtime translation loading vs build-time locale baking, with tradeoffs
- SSR + i18n: `withI18nSupport()` in the client hydration provider list (cross-link to `ssr-and-performance.md`)

`SKILL.md` hookup:

- New short prose section ending with `> Read [references/i18n.md]` pointer
- New `Gotchas` entry: hydrated i18n apps must use `withI18nSupport()` or hydration breaks on translated text nodes
- New line in the `References` list

Source verification: query angular.dev via context7 for every CLI command, provider, and API.

## Acceptance criteria

- [ ] `references/i18n.md` created, density matches the animations pilot
- [ ] Only stable `@angular/localize` APIs covered
- [ ] All commands and provider names verified against angular.dev via context7
- [ ] `SKILL.md` updated: new prose section, new Gotcha, new References list line
- [ ] No content changes to unrelated `SKILL.md` sections
- [ ] No experimental APIs leaked in

## Blocked by

- Issue #01 (animations pilot) — for style precedent
